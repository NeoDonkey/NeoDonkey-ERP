# Journal Entry: Internal Red Team, Gate Condition 9 to Partial

**Date:** 2026-08-21

## Job Selected
- **Job:** Gate condition 9, "An adversary tried": run the internal red-team half as a real adversarial test suite, report honestly, move the gate from red to partial. The external audit half cannot be done by a software session and stays open.

## What Was Done
1. **Read the contract first.** AGENTS.md non-negotiables, docs/GATE.md, docs/CONTRACT.md, then the attack surface: runtime/git (signing, object store, packs), runtime/identity, runtime/polism, runtime/crypto, runtime/money, and the kernel.
2. **Wrote `test/redteam.test.js`: 23 attacks, each with a control assertion.** Forged commits (stranger key, mutated payload, replayed signature, stripped signature), tampered history (flipped byte; well-formed loose-object substitution), rule bypass (widened claim, phantom role, unrecorded roles, uncovered operation, empty claim), ledger unbalance (one cent short, locked period, float amount, hidden precision), encryption breach (non-member open, AAD header transplant, plaintext harvesting, shredded-document reads, sealing downgrade), and float injection at the money API.
3. **Found two real things, both severity low, both kept as failing tests named FINDING:**
   - F-1: `objectStore.read` does not re-hash loose objects; a well-formed forged replacement is served under the honest name. Repack refuses it, `git fsck --strict` names it, sync peers reject it, but the loose read path is silent.
   - F-2: `readSettings` in the kernel calls `normalizeSealedTable`, which is defined nowhere; reopening a workspace with a `sealed` option crashes with ReferenceError, so the sealing-policy comparison is dead code. It fails closed by accident.
4. **Reported** in `docs/security-redteam-2026-08-21.md`: scope, method, per-attack defenses with code paths, the two findings, and the explicit statement that the external audit remains open and required.
5. **Gate flip:** condition 9 red to partial in `test/gate-score.test.js`, evidence `test/redteam.test.js` and the report, missing: external security audit by an independent party. `docs/GATE.md` regenerated from the test.

## Verification
- `test/redteam.test.js`: 23 tests, 21 pass, 2 fail (the two FINDING tests, intentionally).
- Full suite: unchanged apart from the two intentional findings and the three pre-existing failures on main (two h-release tmp-debug.mjs, one readme-datev-claim), being fixed in parallel.

## What Was Not Done
- No external audit was performed or claimed. The gate cannot go green until an independent party audits.
